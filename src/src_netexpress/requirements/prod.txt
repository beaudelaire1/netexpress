# =========================
# Production requirements
# =========================

# Base Django & libs communes
-r base.txt

# Serveur WSGI
gunicorn>=22.0

# PDF (standard unique)
weasyprint>=62.0
Pillow>=10.0

# Base de données (si PostgreSQL sur Render)
psycopg[binary]>=3.1

# Sécurité & utilitaires
whitenoise>=6.6
django-environ>=0.11

# (optionnel mais recommandé)
# pour une meilleure gestion des dates / fuseaux
pytz>=2024.1
