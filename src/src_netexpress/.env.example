DEBUG=1
SECRET_KEY=change-me
ALLOWED_HOSTS=127.0.0.1,localhost
TIME_ZONE=America/Martinique
LANGUAGE_CODE=fr-fr
DJANGO_SETTINGS_MODULE=netexpress.settings.dev
# DATABASE_URL laissé vide = SQLite en dev
# DATABASE_URL=postgres://user:password@db:5432/netexpress

# Paramètres SMTP pour l'envoi d'e‑mails (notifications, factures, devis).
# Pour la production, utilisez Brevo (ex-Sendinblue) SMTP Relay.
# En développement, vous pouvez utiliser Gmail ou laisser vide pour utiliser
# la console backend (les emails s'affichent dans le terminal).

# Email backend (ne modifiez pas si vous utilisez SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# === Configuration Brevo (Production recommandée) ===
# Serveur SMTP Brevo
EMAIL_HOST=smtp-relay.brevo.com
# Port SMTP : 587 pour STARTTLS (recommandé pour Brevo)
EMAIL_PORT=587
# Activation de STARTTLS (True pour Brevo)
EMAIL_USE_TLS=True
# Activation de SSL implicite (False pour Brevo)
EMAIL_USE_SSL=False
# Identifiants de connexion Brevo (votre email Brevo et votre clé API SMTP)
EMAIL_HOST_USER=votre-email@example.com
EMAIL_HOST_PASSWORD=votre-cle-api-smtp-brevo

# === Configuration Gmail (Développement) ===
# Décommentez les lignes ci-dessous si vous utilisez Gmail en développement
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=465
# EMAIL_USE_SSL=True
# EMAIL_USE_TLS=False
# EMAIL_HOST_USER=votre-email@gmail.com
# EMAIL_HOST_PASSWORD=votre-mot-de-passe-application-gmail

# Expéditeur par défaut (utilisez la même adresse que EMAIL_HOST_USER)
DEFAULT_FROM_EMAIL=
# Destinataire pour le formulaire de contact (laisser vide = DEFAULT_FROM_EMAIL)
CONTACT_RECEIVER_EMAIL=
# Destinataire pour les notifications de tâches (laisser vide = DEFAULT_FROM_EMAIL)
TASK_NOTIFICATION_EMAIL=