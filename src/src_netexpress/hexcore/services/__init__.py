"""Service layer for the hexcore package.

Application services orchestrate use cases by coordinating domain
entities and ports.  They encapsulate workflows such as invoice
creation and PDF rendering without tying the domain to external
libraries.
"""
