# Example configuration for NetExpress
# Copy this file to `.env` and replace the values with your own.  The
# environment variables are read by ``django-environ`` in
# ``netexpress/settings/base.py``.

# ----------------------------------------------------------------------
# Django core settings
# ----------------------------------------------------------------------
# A long random secret key used for cryptographic signing.  Use
# `python -c 'import secrets; print(secrets.token_urlsafe(50))'` to
# generate a value.  Do not commit the generated key to source control.
DJANGO_SECRET_KEY=replace-this-with-a-random-secret-key

# Set to `True` only during local development.  In production leave it
# unset or set to `False` to disable debug mode.
DJANGO_DEBUG=True

# Comma‑separated list of host names that this site can serve.  Do not
# include wildcard entries in production.  Example: example.com,www.example.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1


# ----------------------------------------------------------------------
# Database settings
# ----------------------------------------------------------------------
# You can specify a PostgreSQL database by defining a DATABASE_URL.  If
# left unset, Django uses SQLite by default.
# DATABASE_URL=postgres://user:password@localhost:5432/netexpress

# ----------------------------------------------------------------------
# Email settings
# ----------------------------------------------------------------------
# SMTP host used to send transactional e‑mails.  The default works
# with Infomaniak's service; change it for your provider.
EMAIL_HOST=mail.infomaniak.com
EMAIL_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True

# Credentials for the SMTP server.  These should match the mailbox
# configured to send notifications (e.g. noreply@example.com).  Do not
# commit real passwords to source control.
EMAIL_HOST_USER=noreply@example.com
EMAIL_HOST_PASSWORD=change-me

# E‑mail address used in the "From" header when sending mail.  Defaults
# to EMAIL_HOST_USER if not specified.
DEFAULT_FROM_EMAIL=noreply@example.com

# Address where notifications about task updates should be sent.  If
# unspecified, defaults to DEFAULT_FROM_EMAIL.
TASK_NOTIFICATION_EMAIL=admin@example.com